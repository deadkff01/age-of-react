import styled from "styled-components";

export const CivilizationsList = styled.div`
  width: 800px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  text-align: center;
`;
